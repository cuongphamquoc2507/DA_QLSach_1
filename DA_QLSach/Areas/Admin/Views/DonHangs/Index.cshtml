@model IEnumerable<DA_QLSach.Areas.Admin.Data.DonHang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAD.cshtml";
}

<h2>Đơn hàng</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@


<table class="table">
    <tr>
        <th>
            Tên khách hàng
        </th>
        <th>
            Ngày đặt
        </th>
        <th>
            Tình trạng
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.KhachHang.TenKH)

    </td>
    <td>
        @Html.DisplayFor(modelItem => item.NgayDat)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TinhTrang)
    </td>
    <td>
        @Html.ActionLink("Sửa", "Edit", new { id = item.IDHoaDon }) |
        @Html.ActionLink("Chi tiết", "Details", new { id = item.IDHoaDon }, new { @style = "color:blue;" })|
        @Html.ActionLink("Xóa", "Delete", new { id = item.IDHoaDon })
    </td>
    
</tr>
}

</table>
